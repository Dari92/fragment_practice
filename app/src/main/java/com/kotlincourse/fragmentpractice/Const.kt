package com.kotlincourse.fragmentpractice

val mFragmentNumber: String = "fragment_number" //this is just to keeping the same string inside of my constant this variable is the companion object added in args.puInt
